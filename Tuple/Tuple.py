Tuple :Collection of the ordered elements 
it is a immutable:
Tuple : more faster then list 
#Allow duplicates 
t =(1,2,3)
feature list tuple 
features               list        tuple 
 mutable                yes         No 
 duplicates              yes         yes 
 performance              slow        fast 
 syntax                    []          () 
  

  t = (1,2,3,4)
  print(t)
  print(t[0])-----> the  access of the tuple of the by using " [index value]" 
  # packing & unpacking
  packing ---> this means we are assig the values to the on  variable (ex: t = (1,2,3,4)) 
  unpacking----> this  means we are assign the values to the different variables 
  Ex : t = (1,2,3)
        a,b,c = t --->here the a=1,b=2,c=3 this is unpacking 
interview ***
1.Why we use tuple when we have  list 
because of the fixed data we use tuple 
2.can tuple consist of list ?
  yes 
3. what is the difference between the append() extend() 
append() : individual elements 
extend() : multiple elements 
4.sort   Vs sorted
sort it gives the assending order  of the list 
sorted it creates separte list 

