# the message contain the  word of "win money" identy it is a spam message
# In python we want to convert the upper case string into lower case th e function is
#         variablename.lower()
# ---> (is) checks the identity  operator that means it checks memory allocation

# ----?(in)  checks the values for examples  a = [1,2,3]
#                                             b = [1,2,3]
#                                             a in b --> identity  here it checks that the value a is in the set of b or not 
# identity -----> is,is not
# memebership operator ------>
message = input()
if win money in message :
     print("spam is detected in the message")
else :
     print("the message is not  the spam")


 